# Metro resolves from the project root; pnpm's isolated layout hides
# transitive React Native packages (e.g. @react-native/normalize-colors).
public-hoist-pattern[]=*react-native*
public-hoist-pattern[]=*@react-native/*
